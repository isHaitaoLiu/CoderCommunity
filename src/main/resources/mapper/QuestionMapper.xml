<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cug.cs.codercommunity.mapper.QuestionMapper">
    <insert id="insertQuestion">
        insert into question(title, description, gmt_create, gmt_modified, creator, tag)
        values (#{title}, #{description}, #{gmtCreate}, #{gmtModified}, #{creator}, #{tag})
    </insert>
    <select id="selectAllQuestion" resultType="cug.cs.codercommunity.model.Question">
        select * from question
    </select>
    <update id="updateQuestion">
        update question
        set title = #{title}, description = #{description}, tag = #{tag}, gmt_modified = #{gmtModified}
        where id = #{id}
    </update>
    <select id="selectRelated" resultType="cug.cs.codercommunity.model.Question">
        select * from question where id != #{id} and tag regexp #{tag}
    </select>

</mapper>